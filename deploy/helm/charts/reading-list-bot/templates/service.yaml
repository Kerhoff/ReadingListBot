apiVersion: v1
kind: Service
metadata:
  name: {{ include "kerhoff-reafing-list-bot.fullname" . }}
  labels:
    {{- include "kerhoff-reafing-list-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kerhoff-reafing-list-bot.selectorLabels" . | nindent 4 }}
