apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kerhoff-reafing-list-bot.fullname" . }}-test-connection"
  labels:
    {{- include "kerhoff-reafing-list-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kerhoff-reafing-list-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
